# ==============================================================================
# ACE Role Screening Portal - Environment Variables Template
# ==============================================================================
# Copy this file to .env.local and fill in the values for your environment
# DO NOT commit .env.local to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Configuration (Public - exposed to browser)
# ------------------------------------------------------------------------------
NEXT_PUBLIC_APP_NAME="ACE Role Screening Portal"

# File Upload Settings (Public)
# Maximum file size in bytes (default: 5MB = 5242880 bytes)
NEXT_PUBLIC_MAX_FILE_SIZE=5242880

# Allowed file types for resume uploads
NEXT_PUBLIC_ALLOWED_FILE_TYPES=".pdf,.doc,.docx"

# ------------------------------------------------------------------------------
# Storage Configuration (Server-side only - NOT exposed to browser)
# ------------------------------------------------------------------------------
# Leave commented to use default paths (./data, ./data/applications, ./data/resumes)
# Uncomment and customize for production deployments

# Base data directory
# DATA_DIR=/path/to/data

# Applications JSON storage directory
# APPLICATIONS_DIR=/path/to/data/applications

# Resume files storage directory
# RESUMES_DIR=/path/to/data/resumes

# Logs directory
# LOGS_DIR=/path/to/data/logs

# ------------------------------------------------------------------------------
# Rate Limiting Configuration (Server-side only)
# ------------------------------------------------------------------------------
# Maximum requests per IP address per time window
RATE_LIMIT_MAX_REQUESTS=10

# Time window in milliseconds (default: 600000 = 10 minutes)
RATE_LIMIT_WINDOW_MS=600000

# ------------------------------------------------------------------------------
# Environment
# ------------------------------------------------------------------------------
# Options: development, production, test
NODE_ENV=development

# ------------------------------------------------------------------------------
# Production-Only Settings (Add when deploying to GCCH)
# ------------------------------------------------------------------------------
# Uncomment and configure for production/GCCH deployment

# Azure Key Vault (GCCH)
# AZURE_KEY_VAULT_URL=https://your-keyvault.vault.usgovcloudapi.net/
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret

# Azure Log Analytics (GCCH)
# AZURE_LOG_ANALYTICS_WORKSPACE_ID=your-workspace-id
# AZURE_LOG_ANALYTICS_SHARED_KEY=your-shared-key

# Data Encryption
# ENCRYPTION_KEY=base64-encoded-256-bit-key-here
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Trusted Proxy IPs (comma-separated for rate limiting)
# TRUSTED_PROXIES=10.0.0.1,10.0.0.2

# ==============================================================================
# For local development, copy this file to .env.local:
#   cp .env.example .env.local
#
# Then customize the values for your local environment
# ==============================================================================
